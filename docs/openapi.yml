# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service test -Dtest=OpenApiSchemaExportTest` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    AddAttachmentRequest:
      type: object
      required:
      - label
      - source
      - dataBase64
      properties:
        mimeType:
          type: string
        label:
          type: string
          pattern: \S
        source:
          type: string
          pattern: \S
        dataBase64:
          type: string
          pattern: \S
    AgentActivityState:
      type: string
      enum:
      - IDLE
      - BUSY
      - WAITING
      - ENDED
    ApiError:
      type: object
      description: "The error envelope: a message, plus a reason on a 409."
      properties:
        message:
          type: string
          description: "What went wrong, in words. Always present."
        reason:
          $ref: "#/components/schemas/Reason"
          type: string
          description: "Which refusal this is. Present on the 409s the release and\
            \ integrate flows raise, absent on every other error. A value a client\
            \ does not recognise is shown as the message verbatim."
        conflicts:
          type: array
          items:
            type: string
          description: The conflicted paths. Present only for CONFLICT and MERGE_CONFLICT.
    BootstrapOutcome:
      type: string
      enum:
      - SKIPPED
      - SUCCEEDED
      - FAILED
    BootstrapRunDto:
      type: object
      properties:
        bootstrapCommandId:
          type: string
        commandName:
          type: string
        outcome:
          $ref: "#/components/schemas/BootstrapOutcome"
        commandId:
          type: string
        exitCode:
          type: integer
          format: int32
        ranAt:
          $ref: "#/components/schemas/Instant"
    BranchResolution:
      type: object
      properties:
        resolved:
          type: boolean
        workspaceId:
          type: integer
          format: int64
    BranchSweepReport:
      type: object
      properties:
        dryRun:
          type: boolean
        repositoriesExamined:
          type: integer
          format: int32
        branchesExamined:
          type: integer
          format: int32
        removed:
          type: array
          items:
            $ref: "#/components/schemas/SweptBranch"
        errors:
          type: array
          items:
            $ref: "#/components/schemas/SweepError"
    CleanupBranchRequest:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
        result:
          type: string
    CreateWorkspaceRequest:
      type: object
      required:
      - repositoryId
      - id
      properties:
        repositoryId:
          type: string
          pattern: \S
        id:
          type: string
          pattern: \S
        parent:
          type: string
        branch:
          type: string
        preamble:
          type: string
        adoptExisting:
          type: boolean
        branchTree:
          type: boolean
        admin:
          type: boolean
    DiscardWorkspaceRequest:
      type: object
      properties:
        result:
          type: string
    Entry:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Entry1:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDto"
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    IntegrateRequest:
      type: object
      required:
      - summary
      properties:
        summary:
          type: string
          maxLength: 100
          pattern: \S
    LaunchPin:
      type: object
      properties:
        image:
          type: string
        version:
          type: string
        launches:
          type: string
    LaunchPins:
      type: object
      properties:
        generatedAt:
          $ref: "#/components/schemas/Instant"
        pins:
          type: array
          items:
            $ref: "#/components/schemas/LaunchPin"
    MergeBranchRequest:
      type: object
      required:
      - source
      properties:
        source:
          type: string
          pattern: \S
        target:
          type: string
        result:
          type: string
    MergeWorkspaceRequest:
      type: object
      properties:
        target:
          type: string
    PromptAttachmentSource:
      type: string
      enum:
      - SKETCH
      - PASTE
    Reason:
      type: string
      enum:
      - CONFLICT
      - MERGE_CONFLICT
      - NOT_FAST_FORWARD
      - ALREADY_INTEGRATED
      - PUSH_REJECTED
      - RELEASE_REQUIRED
    ResolveReleasedBranchRequest:
      type: object
      required:
      - branch
      properties:
        branch:
          type: string
          pattern: \S
        target:
          type: string
        commit:
          type: string
        result:
          type: string
    Response:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
        cleanedUp:
          type: boolean
    Response1:
      type: object
      properties:
        success:
          type: boolean
    Response10:
      type: object
      properties:
        success:
          type: boolean
    Response11:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Response12:
      type: object
      properties:
        technicalProcessId:
          type: string
    Response13:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response14:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry1"
    Response15:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceHistoryDetailDto"
    Response16:
      type: object
      properties:
        id:
          type: string
        mimeType:
          type: string
        label:
          type: string
        source:
          $ref: "#/components/schemas/PromptAttachmentSource"
        createdAt:
          $ref: "#/components/schemas/Instant"
    Response17:
      type: object
      properties:
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/WorkspacePromptAttachmentDataDto"
    Response18:
      type: object
      properties:
        draft:
          $ref: "#/components/schemas/WorkspacePromptDraftDto"
    Response19:
      type: object
      properties:
        draft:
          $ref: "#/components/schemas/WorkspacePromptDraftDto"
    Response2:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ServiceEventDto"
    Response3:
      type: object
      properties:
        runs:
          type: array
          items:
            $ref: "#/components/schemas/BootstrapRunDto"
    Response4:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    Response5:
      type: object
      properties:
        commitHash:
          type: string
        hasConflicts:
          type: boolean
        output:
          type: string
    Response6:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/Entry"
    Response7:
      type: object
      properties:
        commitSha:
          type: string
        branch:
          type: string
        targetBranch:
          type: string
    Response8:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
    Response9:
      type: object
      properties:
        workspace:
          $ref: "#/components/schemas/WorkspaceDto"
        technicalProcessId:
          type: string
    SavePromptDraftRequest:
      type: object
      required:
      - content
      properties:
        content:
          type: string
        serializedPrompt:
          type: string
    ServiceEventDto:
      type: object
      properties:
        repoId:
          type: string
        workspaceId:
          type: string
        workspaceRowId:
          type: integer
          format: int64
        serviceId:
          type: string
        serviceName:
          type: string
        kind:
          $ref: "#/components/schemas/ServiceEventKind"
        severity:
          $ref: "#/components/schemas/ServiceEventSeverity"
        status:
          $ref: "#/components/schemas/ServiceStatus"
        summary:
          type: string
        logExcerpt:
          type: string
        commandId:
          type: string
        source:
          type: string
        anchorFrom:
          type: integer
          format: int64
        anchorTo:
          type: integer
          format: int64
        sourceEpoch:
          $ref: "#/components/schemas/Instant"
        timestamp:
          $ref: "#/components/schemas/Instant"
    ServiceEventKind:
      type: string
      enum:
      - STATUS_CHANGED
    ServiceEventSeverity:
      type: string
      enum:
      - INFO
      - WARNING
      - ERROR
    ServiceStatus:
      type: string
      enum:
      - STARTING
      - READY
      - RESTARTING
      - CRASHED
      - STOPPED
    SweepBranchesRequest:
      type: object
      required:
      - repositories
      properties:
        dryRun:
          type: boolean
        repositories:
          type: array
          items:
            $ref: "#/components/schemas/SweepRepository"
        keepPrefixes:
          type: array
          items:
            type: string
    SweepError:
      type: object
      properties:
        repositoryId:
          type: string
        branch:
          type: string
        error:
          type: string
    SweepRepository:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        mainBranch:
          type: string
    SweptBranch:
      type: object
      properties:
        repositoryId:
          type: string
        repositoryName:
          type: string
        branch:
          type: string
    UpdateHistoryRequest:
      type: object
      properties:
        preamble:
          type: string
        result:
          type: string
    WorkspaceCommandDto:
      type: object
      properties:
        id:
          type: string
        actionName:
          type: string
        status:
          type: string
        exitCode:
          type: integer
          format: int32
        launchedAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        branch:
          type: string
        repositoryMainBranch:
          type: string
        ahead:
          type: integer
          format: int32
        behind:
          type: integer
          format: int32
        conflictsWithParent:
          type: boolean
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        runtimeStatus:
          $ref: "#/components/schemas/WorkspaceRuntimeStatus"
        runtimeError:
          type: string
        clean:
          type: boolean
        agentActivity:
          $ref: "#/components/schemas/AgentActivityState"
        preamble:
          type: string
        result:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        daemonConnectedAt:
          $ref: "#/components/schemas/Instant"
        daemonVersion:
          type: string
        daemonBuildTime:
          $ref: "#/components/schemas/Instant"
        daemonOutdated:
          type: boolean
        admin:
          type: boolean
    WorkspaceEventDto:
      type: object
      properties:
        type:
          $ref: "#/components/schemas/WorkspaceEventType"
        branch:
          type: string
        parent:
          type: string
        target:
          type: string
        commit:
          type: string
        note:
          type: string
        at:
          $ref: "#/components/schemas/Instant"
    WorkspaceEventType:
      type: string
      enum:
      - CREATED
      - MERGED
      - UPDATED_FROM_PARENT
      - INTEGRATED
      - ABANDONED
    WorkspaceHistoryDetailDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        preamble:
          type: string
        result:
          type: string
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
        events:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceEventDto"
        commands:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceCommandDto"
    WorkspaceHistoryDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        workspaceId:
          type: string
        parent:
          type: string
        status:
          $ref: "#/components/schemas/WorkspaceStatus"
        createdAt:
          $ref: "#/components/schemas/Instant"
        resolvedAt:
          $ref: "#/components/schemas/Instant"
    WorkspacePromptAttachmentDataDto:
      type: object
      properties:
        id:
          type: string
        mimeType:
          type: string
        label:
          type: string
        source:
          $ref: "#/components/schemas/PromptAttachmentSource"
        createdAt:
          $ref: "#/components/schemas/Instant"
        dataBase64:
          type: string
    WorkspacePromptDraftDto:
      type: object
      properties:
        content:
          type: string
        serializedPrompt:
          type: string
        promptVersion:
          type: integer
          format: int64
        lastRunAt:
          $ref: "#/components/schemas/Instant"
        lastRunPromptVersion:
          type: integer
          format: int64
        lastRunCommandId:
          type: string
        updatedAt:
          $ref: "#/components/schemas/Instant"
    WorkspaceRuntimeStatus:
      type: string
      enum:
      - RUNNING
      - STOPPED
      - PROVISIONING
      - FAILED
    WorkspaceStatus:
      type: string
      enum:
      - ACTIVE
      - INTEGRATED
      - ABANDONED
  securitySchemes:
    SecurityScheme:
      type: openIdConnect
      openIdConnectUrl: http://qits-platform-idp:8080/idp/.well-known/openid-configuration
      description: Authentication
paths:
  /workspaces/api/branches/cleanup:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CleanupBranchRequest"
        required: true
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response1"
        "400":
          description: "The branch is not eligible: uncommitted changes, unmerged\
            \ commits, or dependents."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Cleanup Branch
      tags:
      - Branch Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/branches/merge:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeBranchRequest"
        required: true
      responses:
        "200":
          description: Merged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          description: "A blank, dash-leading or self-referential branch name."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The target is the repository's default branch. `reason` is
            RELEASE_REQUIRED and the message names the release flow that does write
            it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Merge Branch
      tags:
      - Branch Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/branches/resolution:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveReleasedBranchRequest"
        required: true
      responses:
        "200":
          description: "Resolved, or `resolved:false` when no active workspace stood\
            \ on the branch — the ordinary case, and what a repeated call answers."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BranchResolution"
        "400":
          description: "A blank branch, or the repository's main workspace — refused\
            \ on both belts: the row has no parent, or the branch is the repository's\
            \ default branch."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Resolve Released Branch
      tags:
      - Branch Resolution Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /workspaces/api/editor/ensure:
    post:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "201":
          description: The editor was started by this call.
        "200":
          description: The editor was already there.
          content:
            application/json:
              schema: {}
        "400":
          description: "The repository is not a project's wrapper, so it has no editor."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such repository.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Ensure
      tags:
      - Editor Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/gc/branches:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SweepBranchesRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BranchSweepReport"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Sweep Branches
      tags:
      - Gc Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /workspaces/api/history:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response14"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Workspace History Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/history/{id}:
    patch:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHistoryRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response13"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Update
      tags:
      - Workspace History Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response15"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Workspace History Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/pins:
    get:
      summary: The container images a launch by this service would pull right now
      responses:
        "200":
          description: "The effective launch pins, image order"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LaunchPins"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Pins Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /workspaces/api/service-events:
    get:
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          format: int32
          default: 0
          minimum: 0
      - name: pageSize
        in: query
        schema:
          type: integer
          format: int32
          default: 50
          maximum: 500
          minimum: 1
      - name: repoId
        in: query
        schema:
          type: string
      - name: severity
        in: query
        schema:
          $ref: "#/components/schemas/ServiceEventSeverity"
      - name: since
        in: query
        schema:
          $ref: "#/components/schemas/Instant"
      - name: source
        in: query
        schema:
          type: string
      - name: workspaceId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response2"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Service Event Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces:
    get:
      parameters:
      - name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response6"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response11"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Create
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: The workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response8"
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/active-process:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response12"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Active Process
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/bootstrap-runs:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: The last run of each bootstrap step.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response3"
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Workspace Bootstrap Run Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/delete-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete Container
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/discard:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: ignore-changes
        in: query
        schema:
          type: boolean
          default: false
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DiscardWorkspaceRequest"
        required: true
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response10"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Discard
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/ensure-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response9"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Ensure Container
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/integrate:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntegrateRequest"
        required: true
      responses:
        "200":
          description: Merged into the parent branch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response7"
        "400":
          description: "No summary, an oversized one, or a workspace with no branch\
            \ to integrate."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: "Nothing landed and the target is unchanged — including when\
            \ the parent is the default branch, which only the release flow writes.\
            \ `reason` says which refusal."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Integrate
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/merge:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MergeWorkspaceRequest"
        required: true
      responses:
        "200":
          description: Merged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response5"
        "409":
          description: "The target is the repository's default branch. Use /integrate,\
            \ or ask qits-projects for a release request."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      summary: Merge
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/prompt-attachments:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: "The attachments, oldest first."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response17"
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: List
      tags:
      - Workspace Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddAttachmentRequest"
        required: true
      responses:
        "201":
          description: Attached; the row without its bytes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response16"
        "400":
          description: "Not valid base64, not a PNG or JPEG, or an unknown source."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: The image exceeds the configured per-image byte cap.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Add
      tags:
      - Workspace Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/prompt-attachments/{attachmentId}:
    put:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddAttachmentRequest"
        required: true
      responses:
        "200":
          description: Updated; the same row id is preserved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response16"
        "400":
          description: "Not valid base64, not a PNG or JPEG, or an unknown source."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: "No such ACTIVE workspace, or no such attachment on it."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: The image exceeds the configured limit.
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Update
      tags:
      - Workspace Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: Removed.
        "404":
          description: "No such ACTIVE workspace, or no such attachment on it."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Workspace Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/prompt-attachments/{attachmentId}/content:
    get:
      parameters:
      - name: attachmentId
        in: path
        required: true
        schema:
          type: string
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: The stored PNG or JPEG bytes.
          content:
            image/png: {}
            image/jpeg: {}
        "404":
          description: "No such ACTIVE workspace, or no such attachment on it."
          content:
            image/png:
              schema:
                $ref: "#/components/schemas/ApiError"
            image/jpeg:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Content
      tags:
      - Workspace Prompt Attachment Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/prompt-draft:
    put:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavePromptDraftRequest"
        required: true
      responses:
        "200":
          description: "The persisted draft, with its fresh updatedAt."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response18"
        "400":
          description: The content is not a well-formed JSON document.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: The draft exceeds the configured byte cap.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Save
      tags:
      - Workspace Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: The saved draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response19"
        "404":
          description: "No such ACTIVE workspace, or it has no saved draft."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Get
      tags:
      - Workspace Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
    delete:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "204":
          description: There is no draft for this workspace.
        "404":
          description: No such ACTIVE workspace.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Delete
      tags:
      - Workspace Prompt Draft Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/recreate-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response4"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Recreate Container
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
  /workspaces/api/workspaces/{id}/stop-container:
    post:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
          format: int64
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkspaceDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      summary: Stop Container
      tags:
      - Workspace Controller
      security:
      - SecurityScheme:
        - qits:admin
info:
  title: qits-workspaces API
  version: 2026.905.65332
